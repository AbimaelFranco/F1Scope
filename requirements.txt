Flask>=3.0,<4.0
requests>=2.31,<3.0

# Production WSGI server (the Docker image's CMD) — Flask's own dev server
# explicitly warns it isn't meant for this. Not needed for local
# `flask run`/`python wsgi.py`, but it's a runtime (not dev-only)
# dependency, so it belongs here rather than requirements-dev.txt.
gunicorn>=21.2,<23.0
